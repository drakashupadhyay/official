# Dr. Akash Kumar Upadhyay - portfolio + enquiry form

The enquiry form uses a standard HTML `mailto:` action. Submitting it opens the visitor's configured email
application with the enquiry details; no client-side API or Netlify function is required.

## Mobility Cure
Logo file: `mobility-cure-logo.png` (cropped from the clinic letterhead). Replace it with the original high-resolution
file if you have one, keeping the same file name, for a sharper logo.
